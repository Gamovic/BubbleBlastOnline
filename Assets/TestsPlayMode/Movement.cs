/*using UnityEngine;

public class Movement
{
    public float moveSpeed;

    public Movement(float speed)
    {
        moveSpeed = speed;
    }

    public Vector2 Calculate(float h, float v, float deltaTime)
    {
        var x = h * moveSpeed * deltaTime;
        var y = v * moveSpeed * deltaTime;

        return new Vector2(x, y);
    }
}*/